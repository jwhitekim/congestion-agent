# 콘다 환경 생성
conda create -n name python=3.10 -y
conda activate name

# 기본 과학 계산 및 GUI 패키지 설치
conda install -c conda-forge numpy scipy matplotlib pillow networkx sympy -y
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
pip install ultralytics
pip install filterpy
pip install scikit-learn
pip install opencv-python

# 문제가 되었던 lap 패키지 설치 (conda-forge 채널 사용)
conda install -c conda-forge lap pyqt -y

# 2단계: pip으로 나머지 패키지 설치
pip install chromedriver-autoinstaller requests selenium selenium-stealth django python-decouple 

# 3단계: ocsort만 따로 설치
pip install ocsort --no-deps

# OSError: [WinError 182] 운영 체제가 %1을(를) 실행할 수 없습니다. Error loading "C:\Users\prml-s-j.hong\anaconda3\envs\joon\lib\site-packages\torch\lib\fbgemm.dll" or one of its dependencies.
conda remove pytorch torchvision torchaudio cpuonly -y
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
python -c "import torch; print(torch.__version__); print(torch.cuda.is_available())"


